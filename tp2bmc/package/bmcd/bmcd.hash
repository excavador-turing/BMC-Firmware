# bmcd is a cargo package: Buildroot fetches the GitHub archive, runs
# `cargo vendor`, and stores the re-packed archive (the "-cargo2" suffix)
# -- so the sum covers the vendored crates too, exactly as Buildroot does
# for its own cargo packages.
#
# THE SUM DEPENDS ON THE RUST TOOLCHAIN, not just on the commit. The
# archive is named after the commit alone, so a stale one in dl/ looks
# right and hashes wrong. Measured on 2026-09-08 against commit 9b5cffa:
# rust-bin 1.85.0 vendored
# 9815f9cb190773112439b8979222f45ee79124f15ace2d29c5484141be679f65,
# rust-bin 1.98.1 vendored
# 84b9825371b085cea0f21ef03efd50893cfb16c0806cecb98c3700d605b44b9c.
# Recompute after any Rust bump, from a tree re-extracted AFTER the bump --
# buildroot_patches/ are applied at extract time, so a tree extracted
# before the patch changed still carries the old toolchain and produces the
# old sum under a name that looks correct.
#
# Computed with buildroot/local.mk REMOVED. A dev override rsyncs a working
# tree instead of downloading the pinned archive, so a hash computed with one
# in place would describe whatever happened to be on the workstation.
#
# The version is commit 53154ed on excavador-turing/bmcd branch `hive`,
# released as v2.32.0. Two releases in one pin:
#
# v2.31.0 -- the daemon offers TLS 1.3, with 1.2 as the fallback. Its acceptor
# was built from Mozilla's v4 intermediate profile, which pins the maximum
# protocol version to TLS 1.2, and under TLS 1.2 a client's supported_groups
# list constrains the curve of the SERVER's certificate as well as the key
# exchange. Envoy's default list stops at P-256, every certificate in this
# estate is P-384, so the gateway could not complete a handshake with a board
# at all. Both boards looked dead behind a working login page.
#
# v2.32.0 -- /metrics reports when the serving certificate expires, and which
# key it is built on. SQU-115 was a board serving a certificate that had
# expired more than a year earlier with nothing anywhere saying so; a number a
# scrape can alert on is the fix for that. It also pins, as a test, that every
# key an operator might install is served: RSA, P-256, P-384, P-521, Ed25519.
#
# This is a listener change. The promotion gate is untouched and no setting
# changes; TLS 1.2 remains available for whatever client is to hand.
#
# Measured 2026-09-11 with rust-bin 1.98.1 in the pinned container, from a
# tree with no buildroot/local.mk: `make bmcd-source` reported got
# c494902e against a zeroed expectation, then re-run to confirm it fetches
# and verifies cleanly.
sha256  c494902ead967b4b12f597c7b3a7449c7f7e246b08776e0cb86e8e87c13bb491  bmcd-53154ed90465b2bd7374c2d04df34a0af68176d4-cargo2.tar.gz
sha256  809fa1ed21450f59827d1e9aec720bbc4b687434fa22283c6cb5dd82a47ab9c0  LICENSE
